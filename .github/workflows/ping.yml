name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel API
        run: |
          curl -s https://render-pinger-xi.vercel.app/api/ping || echo "Ping failed"